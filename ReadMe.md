This module is primarily based on the psycopg 3 Python library to work with PostgreSQL DBMS.

There are two main classes:
- `PostgresConnector` for data definition operations, and
- `MyQuery` for data manipulation operations.

Also there is a third class - `MyLogger` - for handling logging. This class is a helper class based on the Python's default `logging` library.
